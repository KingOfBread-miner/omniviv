services:
  tileserver:
    image: maptiler/tileserver-gl:latest
    container_name: live-tram-tileserver
    ports:
      - "8080:8080"
    volumes:
      - ./bavaria.mbtiles:/data/bavaria.mbtiles:ro
    environment:
      - VERBOSE=true
    restart: unless-stopped
